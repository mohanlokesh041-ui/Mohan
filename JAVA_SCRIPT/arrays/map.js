let number = [1,2,3,4]
        const number1 = number.map((numbers) =>numbers * 1  )
        console.log(number1)

        const number2 = number.map((numbers) => numbers * 2)
        console.log(number2)

        const number3 = number.map((numbers) => numbers * 3)
        console.log(number3)

        const number4 = number.map((numbers) => numbers * 4)
        console.log(number4)

        const number5 = number.map((numbers) => numbers * 5)
        console.log(number5)

        const number6 = number.map((numbers) => numbers * 6)
        console.log(number6)

        const number7 = number.map((numbers) => numbers * 7)
        console.log(number7)

        const number8 = number.map((numbers) => numbers * 8)
        console.log(number8)

        const number9 = number.map((numbers) => numbers * 9)
        console.log(number9)

        const number10 = number.map((numbers) => numbers * 10)
        console.log(number10)

        const number11 = number.map((numbers) => numbers * 11)
        console.log(number11)

        const number12 = number.map((numbers) => numbers * 12)
        console.log(number12)

        const number13 = number.map((numbers) => numbers * 13)
        console.log(number13)

        const number14 = number.map((numbers) => numbers * 14)
        console.log(number14)

        const number15 = number.map((numbers) => numbers * 15)
        console.log(number15)

        const number16 = number.map((numbers) => numbers * 16)
        console.log(number16)

        const number17 = number.map((numbers) => numbers * 17)
        console.log(number17)

        const number18 = number.map((numbers) => numbers * 18)
        console.log(number18)